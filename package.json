{
  "name": "casino-review-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static site generator for casino review sites. Zero dependencies by design.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node build.js",
    "lint": "node build.js --lint-only",
    "serve": "node serve.js"
  },
  "dependencies": {},
  "devDependencies": {}
}